image: Ubuntu

stack: jdk 11

branches:
  only:
    - master

build: off
services:
  - mysql

install:
  - java -jar ./artifacts/aqa-shop.jar
  - ./gate-simulator npm start

artifacts:
  - path: artifacts

build_script:
  - chmod +x gradlew
  - ./gradlew test --info -Dselenide.headless=true