image: Ubuntu

stack: jdk 11

branches:
  only:
    - master

services:
  - mysql

environment:
  nodejs_version: "6"

install:
  - npm install
  - ./Diplom/gate-simulator
  - java -jar ./artifacts/aqa-shop.jar

artifacts:
  - path: artifacts

  - path: gate-simulator
build: off

build_script:
  - chmod +x gradlew
  - ./gradlew test --info -Dselenide.headless=true